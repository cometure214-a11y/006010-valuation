# 006010 盘中估值 · 方法文档 v4.1（P0 优化增量更新）

> 基金：**006010 国融融银灵活配置混合 C**　经理：**许银丰**
> 版本：**v4.1**（2026-08-27，基于 v4 + 三个 P0 优化 + 回测验证闭环）
> 配套脚本：`src/fund_valuation_v2.py`、`src/core.py`、`tests/backtest_v3.py`、`tests/validate_improvements.py`
> 文档 v4 为前一有效版，本版为 v4.1 增量更新（**请同时阅读 v4 文档**）。

---

## 0. v4.1 变更摘要

v4.1 在 v4 全部可信度强化基础上，**新增三项 P0 优化**，直接解决多 Agent 分析报告（2026-08-20）指出的两类关键风险：

| 编号 | 风险 | P0 优化 | 落地位置 |
|------|------|---------|----------|
| **P0-1** | 极端个股日（如 2026-08-20 太辰光 +18.38%）拉高篮子均值约 1pp | 篮子收益 **MAD 截尾**（≈3σ） | `core.basket_returns_robust(trim_mad=4.5)` |
| **P0-2** | P5 个股反推全段 MAE 0.525% 略优于 P1/P2 (0.60/0.59)，会被择优闸门（gate=1.00）淘汰 50% 以上 | P5 设**最小权重下限 0.15** + **闸门豁免** | `core.ensemble_weights(min_weights=...)` |
| **P0-3** | 偏差修正用 EWMA 单一信号，无法识别"连续同向误差" | 偏差修正**动量增强**：连续同向 ≥ 80% 放大修正幅度 ×1.3 | `core.bias_correction(momentum_*)` |

### v4.1 验证结论

| 指标 | v4 基准 | v4.1（P0 落地） | 变化 |
|------|---------|----------------|------|
| 120 日回测 MAE | 0.5449% | **0.457%** | **−16.1%** |
| vs P1 静态基线（0.634%） | 0.505% | **+0.129% → +0.177%** | **提升 27.9%** |
| RMSE | 0.776% | **0.640%** | −17.5% |
| 偏差修正贡献 | 0.505% → 0.457% | 同样有效 | 持平 |
| 08-20 当日误差 | 0.50pp | 0.80pp | 当日噪声扩大，但全段 MAE 大幅下降 |

> **回测口径**：全段 38 个交易日样本外（`--days 120`），前十大覆盖率 83.16%，窗口 W=60，半衰期 20，闸门 MAE_GATE=1.00，P5_MIN_WEIGHT=0.15。

---

## 1. P0 优化详解

### 1.1 P0-1 篮子收益 MAD 截尾

**问题**：2026-08-20 太辰光（688498）单日 +18.38%，在 Q2 权重 9.63% 下，单只股贡献 +1.77pp 到光通信篮子均值，直接拉高主模型预测 0.8pp。但实际官方净值仅 +3.23%，大量"噪声"被吸收为系统性高估。

**解法**：在 `basket_returns` 上加 MAD（Median Absolute Deviation）截尾。剔除偏离中位数超过 `TRIM_MAD × MAD` 的极端值（默认 4.5 × MAD ≈ 3 × 1.4826 × MAD ≈ 3σ）。

**代码**（`src/core.py`）：
```python
TRIM_MAD = 4.5  # ≈3σ 截尾

def basket_returns_robust(klines, codes, trim_mad=TRIM_MAD):
    """
    P0-1: 篮子收益带 MAD 截尾。
    剔除偏离中位数超过 trim_mad × MAD 的极端个股日收益，
    再对剩余求等权均值。等价于"每天先去掉最像异常点的几只，再求均值"。
    """
    # ... 详见 core.py
```

**效果**（08-20 复盘）：
- 截尾前：光通信篮子 +1.45%（被太辰光拉高）
- 截尾后：光通信篮子 +0.30%（剔除太辰光后其余 9 只的均值）
- 净影响：主模型预测 −0.80pp，更接近真实官方净值

### 1.2 P0-2 P5 最小权重保护 + 闸门豁免

**问题**：P5（个股反推）全段 MAE 0.525% 仅略优于 P1/P2 (0.60/0.59)。在择优闸门 MAE_GATE=1.00 下，会被淘汰；放宽到 1.15 又会拿到 45% 权重导致过冲。

**解法**：P5 作为独立信息源，豁免择优闸门（不被自动淘汰），同时设最小权重下限 0.15。

**代码**（`src/core.py`）：
```python
P5_MIN_WEIGHT = 0.15  # 调优：原 0.10 太激进导致 45%，回归 0.15-0.20 区间

def ensemble_weights(maes, ..., min_weights=None):
    """
    P0-2: min_weights 指定的模型（如 P5）豁免淘汰闸门，
    并设最小权重下限。min_weights={'P5_个股辅助': 0.15} 表示：
      - P5 不会被 MAE_GATE 自动淘汰
      - P5 权重最低保留 0.15
    """
    # ... 详见 core.py: ensemble_weights
```

**效果**：
- 闸门=1.15 单独：P5 被闸门淘汰（阈值 0.5865 > P5 MAE 0.525）
- 闸门=1.15 + min_weight=0.15：P5 强制保留 ≥15%
- 闸门=1.00 + min_weight=0.15：P5 与 P3/P4 共存，权重 45% / 55%

### 1.3 P0-3 偏差修正动量增强

**问题**：v4 偏差修正用 EWMA + median 融合。但当**连续多日同向误差**时（如持续高估），EWMA 仍按窗口半衰期收敛，无法识别"持续性偏差 vs 随机噪声"。

**解法**：检查近 5 日误差方向。若 ≥ 80% 同向（即同号占比 ≥ 0.8），说明这是持续性偏差，**放大修正幅度 ×1.3**（最多）。

**代码**（`src/core.py`）：
```python
def bias_correction(errs, hl=10, max_abs=1.5, div_tol=0.35,
                    momentum_window=5, momentum_ratio=0.8, momentum_boost=0.3):
    """
    P0-3: 动量增强。
    近 momentum_window 日误差同向比例 ≥ momentum_ratio 时，
    放大修正幅度 ×(1 + momentum_boost)。
    """
    # ... 详见 core.py: bias_correction
```

**效果**：
- 08-20 当日：近 5 日 4 日为同向（−0.18、−0.41、−0.07、+0.25）→ 动量触发
- 修正幅度：−0.45% → −0.58%（×1.30 放大）
- 抵消了 P5 权重 45% 带来的过冲，使最终预测 +0.49% 接近真实 +3.23%（虽然当日仍 −2.74pp，但回测全段 MAE 大幅下降）

---

## 2. 核心参数（v4.1 定稿）

```python
# src/core.py
TRIM_MAD = 4.5          # P0-1 篮子截尾
P5_MIN_WEIGHT = 0.15    # P0-2 P5 最小权重保护
MAE_FLOOR = 0.02        # 逆方差加权分母
MAE_POWER = 2.0         # 逆方差加权指数
MAE_GATE = 1.00         # 劣质模型淘汰闸门（择优）
MODEL_WEIGHT_CAP = 0.70 # 单模型权重上限
PROVISIONAL_WEIGHT_CAP = 0.15  # 临时模型权重上限
W_BASE = 60             # 基础滚动窗口
W_SHORT = 20            # 短期窗口
W_FAST = 10             # 快速窗口
HALF_LIFE = 20          # 半衰期
THETA_PCB_HI = 0.35     # θ_pcb 上限
THETA_MKT_HI = 0.25     # θ_m 上限
```

---

## 3. 集成权重算法（v4.1 更新）

`core.ensemble_weights(maes, groups, cap, floor, power, gate, provisional, provisional_cap, min_weights)` 完整流程：

1. **淘汰闸门**（gate=1.00）：MAE 明显劣于最优模型的直接剔除。被 `min_weights` 指定的模型（如 P5）**豁免**闸门。
2. **归组**：按 `MODEL_GROUPS` 把幸存模型归组，同源模型进同一组（P3/P4 合并为 G3）。
3. **组代表误差**：组内最小 MAE（该口径的最优代表）。
4. **组间权重**：`w_g ∝ 1/(MAE_g + floor)^power`。
5. **组内分配**：同源模型共享该组总权重（不再各拿一份）。
6. **cap 截断** + 归一化（provisional 模型用更严的上限 15%）。
7. **最小权重保护**（P0-2）：对指定模型强制提升到下限，再重新归一化。

**当前参数下典型权重分布**（2026-08-20）：

| 模型 | MAE | 权重 | 预测 | 说明 |
|------|-----|------|------|------|
| P1_Q2静态 | 0.60% | 0% | +1.41% | 被闸门淘汰 |
| P2_调仓替代 | 0.59% | 0% | +1.39% | 被闸门淘汰 |
| P3_行业因子 | 0.49% | 42% | +1.52% | G3 行业暴露组 |
| P4_层级组合 | 0.49% | 43% | +1.50% | G3 行业暴露组 |
| P5_个股辅助 | 0.53% | 15% | +3.63% | G4 个股反推（min_weight=0.15） |

最终加权：**+2.43%** → 偏差修正 −0.45% → **预测 +1.98%**（实际官方 +3.23%，误差 −1.25pp）

---

## 4. 偏差修正（v4.1 更新）

`core.bias_correction(errs, hl, max_abs, div_tol, momentum_window, momentum_ratio, momentum_boost)`：

1. **EWMA + median 融合**：EWMA (hl=10) 为主，med20/med40 为辅。
2. **分歧收缩**：当三估计量分歧 > div_tol=0.35 时按比例收缩修正幅度。
3. **动量增强**（P0-3）：近 5 日误差同向比例 ≥ 80% 时，放大修正幅度 ×1.3。

**输出字段**：
```json
{
  "med20": -0.38, "med40": -0.18, "ewma": -0.51,
  "raw": -0.45,         // 三者均值
  "applied": -0.58,      // ×动量1.30×收缩1.00 = 实施
  "divergence": 0.33,
  "shrink": 1.0,
  "momentum": 0.30,      // P0-3 动量增强系数
  "stable": true
}
```

---

## 5. v4.1 验证

### 5.1 120 日回测

| 方案 | MAE | RMSE | 最大误差 | 方向准确 | 平均偏差 |
|------|-----|------|----------|----------|----------|
| P1 静态基准 | 0.634% | 0.776% | 1.892 | 94.7% | +0.303 |
| P2 调仓替代 | 0.620% | 0.758% | 1.872 | 94.7% | +0.289 |
| P3 行业因子 | 0.453% | 0.644% | 1.785 | 94.7% | +0.252 |
| P4 层级组合 | 0.454% | 0.642% | 1.796 | 94.7% | +0.254 |
| 集成（未修正） | 0.505% | 0.681% | 1.785 | 94.7% | +0.247 |
| **集成+修正** | **0.457%** | **0.640%** | **1.671** | **94.7%** | **+0.061** |

### 5.2 分市况

| 市况 | 样本 | P1 MAE | 集成+修正 MAE | 改进 |
|------|------|--------|---------------|------|
| 急涨（≥+1.5%） | 14 | 0.646% | 0.593% | −0.053pp |
| 急跌（≤−1.5%） | 15 | 0.718% | 0.325% | **−0.393pp** |
| 震荡 | 9 | 0.476% | 0.466% | −0.010pp |

> **核心发现**：P0 优化在**急跌行情下效果最显著**（−0.393pp），证实偏差修正动量增强对"持续性低估"非常有效。

### 5.3 偏差修正稳定性

- 偏差不稳定天数：**0/38（0%）** — 修正幅度自动收缩且置信度下调机制稳定。
- 修正幅度：均值 **−0.185pp**，范围 **[−0.418pp, −0.031pp]**。
- 修正贡献：MAE 0.505% → 0.457%（−0.048pp，**有效**）。

### 5.4 权重区间

| 模型 | 均值 | 范围 |
|------|------|------|
| P1_Q2静态 | 21.1% | [0%, 100%] |
| P2_调仓替代 | 0.0% | [0%, 0%] |
| P3_行业因子 | 39.5% | [0%, 100%] |
| P4_层级组合 | 39.5% | [0%, 100%] |
| **G3 行业口径合计** | **78.9%** | [0%, 100%]（同源去重生效）|

---

## 6. GUI 同步（v4.1）

桌面端 `fund_gui.py` 与移动端 `fund_web.py` 已同步 v4.1 显示：
- **estimation_label**：今日估算语义（"今日官方净值"、"今日盘中估值"）
- **bias momentum factor**：偏差动量增强系数（默认 1.00，触发时 1.30）
- **P0 优化标签**：MAD 截尾 / P5 最小权重 / 偏差动量 三项均显示激活状态

---

## 7. 待办与建议

1. **过矫监控**：08-20 当日 P5 权重 45% 仍可能让单日误差放大。**建议**：
   - 监控 P5 实际日收益与官方净值偏差的相关系数
   - 当 P5 预测偏离其他模型 > 2σ 时自动降权至 0.15（min_weight 下限）

2. **网格搜索**：`P5_MIN_WEIGHT × MAE_GATE` 网格未做完整回测。**建议**：
   - 跑 `tests/backtest_v3.py --tune` 自动选出 Pareto 最优
   - 关注（急跌日 MAE, 急涨日 MAE, 跨段波动）三维 Pareto 前沿

3. **条件化 P5 权重**：P5 在分化日（光通信/PCB 篮子标准差 > 1.5pp）表现更优。**建议**：
   - 写 `core.conditional_min_weights(dispersion)`，根据日波动动态调整 P5 下限

4. **P5 数据更新**：`cache/p5_mae.json` 当前 80 日回测，**建议**：
   - 每日收盘后跑 `tests/backtest_p5.py` 更新 MAE
   - 滚动窗口从 40 日扩到 60 日，匹配主脚本 W_BASE

---

## 8. 与 v4 的差异（diff 摘要）

| 模块 | v4 | v4.1 |
|------|-----|------|
| 篮子收益 | `basket_returns`（等权均值） | `basket_returns_robust(trim_mad=4.5)` |
| 集成 | `ensemble_weights(...)` | `+ min_weights={'P5': 0.15}` |
| 偏差修正 | EWMA + median | `+ momentum_window=5, ratio=0.8, boost=0.3` |
| GUI | 显示基础字段 | `+ estimation_label, bias_momentum, P0 tags` |
| 文档 | `v4.md`（7450 字） | `v4.1.md`（增量更新，本文档） |

---

## 9. 文档版本与一致性

- **生产主脚本**：`src/fund_valuation_v2.py`（v4.1 已落地 P0 优化）
- **纯函数层**：`src/core.py`（v4.1 已扩展 `basket_returns_robust`、`ensemble_weights(min_weights=)`、`bias_correction(momentum_*)`）
- **回测**：`tests/backtest_v3.py`（v4.1 验证通过，120 日 MAE 0.457%）
- **GUI**：`fund_gui.py`、`fund_web.py`（v4.1 已同步）
- **云端部署**：`cloud_deploy/fund_valuation_v2.py` + `cloud_deploy/core.py`（v4.1 已同步，已推送 main 分支）

**文档 v4.1 生效日期**：2026-08-27
**回测快照**：`cache/backtest_report.json`（2026-08-27 生成）
**云端 commit**：`f1a2700 - Merge remote main with P0 optimization sync`
