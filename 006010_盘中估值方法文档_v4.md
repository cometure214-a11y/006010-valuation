# 006010 盘中估值 · 方法文档 v4（可信度与验证严谨性强化版）

> 基金：**006010 国融融银灵活配置混合 C**　经理：**许银丰**
> 版本：v4（2026-08-21，全面融入《优化意见.txt》9 项可信度强化 + 回测验证闭环）
> 配套脚本：`src/fund_valuation_v2.py`（v4 生产主脚本）、`src/core.py`（纯函数层）、`tests/backtest_v3.py`（滚动样本外回测/选参/决策）、`tests/validate_improvements.py`（改进验证）、`fund_holdings_infer.py`（个股辅助预测）、`fetch_all.py`（数据抓取）、`fund_gui.py`（极简面板）
> 文档 v3 见旧版，本版为当前有效版。

---

## 0. 总原则（最重要，先读）

> **本工具的目标不是精确还原第三季度真实个股持仓，而是尽可能降低盘中估算与收盘官方净值之间的误差。**

- Q2 披露持仓作为初始基准，行业因子模型、PCB 调仓比例模型和个股反推模型作为**不同预测器**；最终结果按近期样本外预测误差加权。
- 输出中的"光通信、PCB、市场"等数值属于**模型有效暴露，不等同于基金真实持仓比例**。
- PCB 目前出现持续为正的模型信号，说明"部分调入 PCB"的假设有支持；但因光通信与 PCB 相关性较高（r≈0.72），**暂不能据此确认真实 PCB 持仓比例**，只输出"调仓概率/有效替代比例"。
- **v4 核心升级**：全部核心算法下沉 `src/core.py` 纯函数层，主脚本/回测/单测三端共用一套逻辑，消除逻辑漂移；引入数据源体检、集成审计轨迹、临时权重封顶、真实 P5 样本外 MAE、基于集成自身历史误差的偏差修正（防滞后）、模型淘汰闸门、显式估算语义字段、日期错配自检。

---

## 1. 今日实测结论（2026-08-20 盘中，v4 口径）

| 项 | 值 |
|---|---|
| 模型中心估计（加权 P1~P5） | **+4.35%** |
| 历史偏差修正（集成自身历史误差融合） | **−0.12%** |
| 预计净值 | **≈0.5601**（上一交易日 0.5384） |
| 经验预测区间（±1.5σ） | **+3.49% ~ +5.21%** |
| 模型分歧 | 0.68 pct（小） |
| PCB 有效替代比例 θ_pcb | **7.1%** |
| PCB 调仓信号 | **中等信号**（θ=7.1%、β=9.6%、增量验证 MAE 下降、多篮子一致） |
| 置信度 | **78/100（高）**（MAE 35/40、分歧 22/25、θ 13/15、偏差 8/10、数据质量 10/10） |

各模型预测：P1 Q2静态 +4.39% | P2 调仓替代 +4.18% | P3 行业因子 +4.36% | P4 层级组合 +4.36% | P5 个股辅助 +3.63%

> 注：P5 真实样本外 MAE 为 0.718%（代理值 0.4% 严重高估），已触发临时权重封顶 15%。

---

## 2. 五模型组合架构（v4）

```
P1  Q2 静态基准      r_q2 = Σ(Q2披露权重 × 今日个股收益)
P2  调仓替代比例     r_q2 + θ_pcb×(r_pcb−r_q2) + θ_m×(r_m−r_q2)
P3  行业因子         β1×光通信 + β2×PCB + β3×半导体 + β4×市场
P4  层级组合         行业总比例(β) × 行业内Q2披露相对权重
P5  个股辅助反推     LASSO/NNLS 今日收益均值（仅作辅助特征）

P_final = Σ w_i × P_i     w_i 由 ensemble_weights() 产出（分组去重+闸门+临时封顶）
r_final = P_final + bias_correction(ensemble_own_errors)  # 关键：用集成自身历史误差
```

### 2.1 P1 · Q2 静态基准（锚）
```
r_q2,t = Σ (Q2披露个股权重 × 个股当日收益)
```
保留为"什么都不假设"的基准。今日 **+4.39%**。

### 2.2 P2 · 调仓替代比例模型
直接估计"从 Q2 组合切换了多少"，而非解释多个高度相关的 β：
```
y_t = r_q2,t + θ_pcb,t × (r_pcb,t − r_q2,t) + θ_m,t × (r_m,t − r_q2,t) + ε_t

θ_pcb = 从 Q2 组合切换到 PCB 组合的有效比例
θ_m   = 从 Q2 组合切换到市场/其他风格的有效比例
```
约束：`0 ≤ θ_pcb ≤ 35%`，`0 ≤ θ_m ≤ 25%`（上限通过历史滚动回测选择，**不代表真实持仓上限**）。

参数估计目标（滚动，半衰期加权，含平滑与收缩）：
```
min  Σ q_k × [y_k − ŷ_k]²
     + λ1×(θ_pcb,t − θ_pcb,t−1)² + λ2×(θ_m,t − θ_m,t−1)² + λ3×θ_pcb,t²
```
- `q_k` 时间权重（越近越高）；`λ1/λ2` 防跳变；`λ3` 防无证据时过度加 PCB；
- **每天只用截至前一交易日的数据估计**；盘中预测用 `θ_{t−1}`。
- **v4 修正**：`fit_theta()` 正确传递 `prev` 参数实现真正平滑（优化意见 §1）。

今日：**θ_pcb=7.1%、θ_m=0%**，P2 预测 **+4.18%**。

### 2.3 P3 · 行业因子模型
```
基金日收益 = β1×光通信 + β2×PCB + β3×半导体 + β4×市场 + ε
```
约束：`βi ≥ 0`、`Σβ ≤ 1`、平滑 + Q2 先验。今日 β：光通信 78.5% / PCB 9.6% / 半导体 0% / 市场 7.4% / 残差 4.5%。P3 预测 **+4.36%**。

### 2.4 P4 · 层级组合反推
行业总比例动态、行业内部权重固定：
```
当前个股权重_i = 当前行业权重_g × Q2时期个股i在行业g内的相对权重
```
- 光通信内部：沿用 Q2 披露相对权重（10 只归一化）；
- PCB 内部：等权；
- **不使用 LASSO 结果直接替换个股权重**。
目的不是还原持仓，而是提高"当日个股差异大"时的预测准确度。P4 今日 **+4.36%**。

> **v4 关键修正**：P4 拥有独立的滚动样本外预测序列与 MAE（优化意见 §4），不再借用 P3 的 MAE。回测显示 P4 独立 MAE 与 P3 极其接近，但两者同源于同一组 β，**集成时归入同一分组 G3 避免双倍计权**（优化意见 §4、§10）。

### 2.5 P5 · 个股辅助反推（定位降级）
读取 `fund_holdings_infer.py` 输出（LASSO/NNLS 今日收益均值），仅作辅助。今日 **+3.63%**。

> **v4 关键修正**：P5 真实样本外 MAE 由 `tests/backtest_p5.py` 逐日重拟合产出（80 样本，2026-04-27~2026-08-20），**真实 MAE = 0.718%**，远大于此前代理值 0.4%（优化意见 §5）。无真实 MAE 前已触发 `PROVISIONAL_WEIGHT_CAP = 15%` 硬性封顶；现有真实 MAE 后纳入集成加权但权重仍受整体闸门约束。

---

## 3. 参数（统一常量，唯一真源 `src/core.py`）

```
基础滚动窗口 W_base：60 个交易日
短期窗口 W_short：   20 个交易日
快速窗口 W_fast：    10 个交易日
时间半衰期：         20 个交易日
```

θ 上限（方法文档 §二；上限由回测选择，不代表真实持仓上限）：
```
THETA_PCB_HI = 0.35
THETA_MKT_HI = 0.25
```

集成参数（方法文档 §十，**由 `tests/backtest_v3.py --tune / --decide` 在 112 日样本上选出**）：
```
MAE_FLOOR            = 0.02      # w ∝ 1/(MAE + MAE_FLOOR)^MAE_POWER
MAE_POWER            = 2.0       # 锐度指数：2.0≈逆方差加权
MAE_GATE             = 1.00      # 劣质模型淘汰闸门：MAE 超过最优模型 1.00 倍的不参与集成（择优模式）
MODEL_WEIGHT_CAP     = 0.70      # 单模型权重上限
PROVISIONAL_WEIGHT_CAP = 0.15    # 无"真实样本外误差"模型的硬上限（防假 MAE 换真权重）
```

> **选参依据（2026-08-21 决策，见 `cache/decide_report.json`）：**
> - 分段验证：前 50% 选参、后 50% 独立验证（禁止同批数据既选参又验证）
> - 全段(112日) MAE 0.5426%，最差子段 0.6220%，跨段波动 0.1588pp
> - 同期 P4 单模型 0.5393%/0.6592%/0.2398pp、P1 静态 0.6084%/0.6220%/0.0272pp
> - 该组合在(全段MAE, 最差子段MAE)双准则下位于帕累托前沿
> - 集成 vs 最优单模型：-0.0033pp（统计无差异）→ **主口径取集成 闸门1.00(择优)，因其跨段波动 0.1588pp 显著小于 P4 单模型 0.2398pp，抗崩能力是集成的核心价值**

模型分组（同源模型归为一组，避免重复计权）：
```
P1_Q2静态       → G1_静态基准
P2_调仓替代     → G2_调仓替代
P3_行业因子     → G3_行业暴露
P4_层级组合     → G3_行业暴露   # 与 P3 同源
P5_个股辅助     → G4_个股反推
```

---

## 4. 数据卫生：正式收盘价 vs 盘中实时价（优化意见 §6，v4 核心基建）

**背景（真实事故）**：取数脚本发现腾讯长窗口日 K 当天缺失时，会用实时行情接口补一条 `out[TODAY]=当前价`。若 14:00 抓取、20:00 官方净值才发布，则当天这条"收盘价"实为 14:00 的盘中价，却会作为正式收盘价进入滚动回归、误差统计与回测的样本，同时污染 β/θ 估计和 MAE —— 属于典型的隐性未来信息/错值混入。

**v4 解决方案（双文件隔离 + 幂等防御性清洗）**：
- `cache/klines.json`：只保存正式收盘价（历史不可变，保证回测可复现）
- `cache/intraday.json`：保存盘中实时价格快照，结构 `{"date": "2026-08-20", "settled": false, "prices": {...}, "unsettled_dates": [...]}`
- **核心函数 `core.load_settled_klines()`**：本模块唯一 I/O 函数，载入 klines 并按 intraday.json 剔除未结算日期。**主脚本/回测/反推/验证脚本全部调用此函数**，消灭"同一份数据在不同脚本里含义不同"的头号可信度问题。
- **幂等防御**：无论上游取数脚本是否已修好，下游一律先剔除。支持两层 `{group:{code:{date:px}}}` 与一层 `{code:{date:px}}` 结构。

---

## 5. 集成权重算法（v4：分组去重 + 闸门 + 临时封顶 + 审计轨迹）

`core.ensemble_weights(maes, groups, cap, floor, power, gate, provisional, provisional_cap)` 完整流程：

1. **淘汰闸门**：MAE 明显劣于最优模型的直接剔除（`gate=1.15`）。被淘汰模型保留键、权重显式为 0，不从字典消失（便于下游求和与审计）。
2. **归组**：按 `MODEL_GROUPS` 把幸存模型归组，同源模型进同一组（P3/P4 合并为 G3）。
3. **组代表误差**：组内最小 MAE（该口径的最优代表）。
4. **组间权重**：`w_g ∝ 1/(MAE_g + floor)^power`。
5. **组内二次分配**：同一公式在组内再分配（同源模型共享该组总权重，不再各拿一份）。
6. **Cap 截断 + 归一化**：单模型 `MODEL_WEIGHT_CAP=70%`；`provisional` 模型用更严的 `PROVISIONAL_WEIGHT_CAP=15%`。
7. **补回被淘汰模型/分组**，权重显式 0（保持键完整，便于 JSON 审计）。

**为何需要闸门**：60 日样本外回测显示，纯 `1/(MAE+floor)` 加权会让 P1/P2 拿到 ~65% 权重，集成 MAE 0.516% 反而输给最优单模型 0.486%。提高 power 只能缓解、不能根治。闸门直接剔除劣质模型，是组合预测里的标准做法。

**集成审计轨迹（v4 新增，输出到 `result.json`）**：
- `gate/power/floor/weight_cap/provisional_cap` 当前生效参数
- `dropped` 被淘汰模型列表、`n_active` 活跃模型数
- `model_mae_used` 各模型实际参与加权的 MAE
- `group_mae` 各分组代表误差
- `p5_mae_source` / `p5_is_real_oos` P5 误差来源与是否真实样本外
- `bias_source` 偏差修正来源（集成自身历史误差，非借用 P3）

---

## 6. 样本外误差与偏差修正（v4：防滞后 + 融合估计）

### 6.1 样本外误差口径
- **P1~P4**：各自滚动样本外预测序列（θ/β/权重/偏差修正一律只用 T−1 及以前数据）
- **P5**：真实样本外 MAE，由 `tests/backtest_v3.py`（或专用脚本）逐日重拟合 LASSO/NNLS 产出，写入 `cache/p5_mae.json`
- **集成**：逐日用"截至前一日的 trailing MAE"定权重，重建集成的历史预测序列，再取其误差。全程只用当日之前信息，无未来泄漏。

### 6.2 偏差修正算法 `core.bias_correction(errs, hl=10, max_abs=1.5, div_tol=0.35)`

输入：样本外误差序列（实际 − 模型，百分数，按时间升序）

三估计量融合：
- `med20`：近 20 日误差中位数
- `med40`：近 40 日误差中位数
- `ewma`：半衰期 10 的指数加权均值（对最近变化更敏感，**抗滞后**）

输出字典：
| 字段 | 含义 |
|---|---|
| med20 / med40 / ewma | 三个基础估计量 |
| raw | 0.5×ewma + 0.5×med20（主辅融合） |
| divergence | 三估计量极差（衡量偏差本身是否稳定） |
| shrink | 分歧 > div_tol 时的收缩系数（下限 0.3） |
| stable | 偏差是否稳定（divergence ≤ div_tol） |
| applied | 实际采用的修正值 = raw × shrink，截断到 ±max_abs |

**关键改进（优化意见 §2）**：
- 主脚本/回测均改用**集成自身的历史误差**做偏差修正（此前主脚本错用 P3 误差）。
- 鸡生蛋问题（集成权重依赖 MAE，MAE 要逐日回看）解法与回测一致：逐日用 trailing MAE 定权重 → 重建集成历史预测 → 取其误差序列 → 偏差修正。

---

## 7. 连续置信度评分 0-100（v4 替代原三档硬阈值）

`core.confidence_score(mae, spread, theta_stability, bias_divergence, data_quality, coverage)`

| 维度 | 满分 | 映射逻辑 |
|---|---|---|
| 样本外 MAE | 40 分 | MAE 0.3%→满分, 1.5%→0 分 |
| 模型分歧 | 25 分 | spread 0.5pp→满分, 4pp→0 分 |
| θ 稳定性 | 15 分 | 变动 0.03→满分, 0.20→0 分 |
| 偏差稳定性 | 10 分 | divergence 0.15→满分, 0.8→0 分 |
| 数据质量+覆盖 | 10 分 | data_quality × 覆盖率修正 |

等级：≥75 高 / 50~74 中等 / <50 低。

今日：78/100（高）—— MAE 35/40、分歧 22/25、θ 13/15、偏差 8/10、数据 10/10。

---

## 8. 数据源体检 / 降级 `core.validate_market_data()`

输出：`{"ok": bool, "score": 0~1, "errors": [], "warnings": [], "detail": {...}}`

- **致命** → `ok=False`，主脚本拒绝输出估值（避免用坏数据误导决策）
- **非致命** → 降级并扣质量分（喂给 confidence_score）
- 检查项：nav 行数/顺序/重复/非正、净值日收益异常、数据新鲜度、篮子完整度、前十大覆盖率

---

## 9. 日期错配自检 `core.check_date_consistency()`

曾出现"nav_prev 取到目标日自身净值"的语义错位 bug。v4 显式自检：
- 基准净值日必须早于目标日（允许更早但绝不允许 ≥ 目标日）
- 若目标日净值已公布，基准必须取它之前那条
- 官方净值日必须等于目标日
- 目标日必须是交易日

---

## 10. 显式估算语义字段（v4 根治"到底估的哪天"）

`core.resolve_estimation_mode(target_date, nav_latest_date, official_published, now)`

返回：
| 字段 | 含义 |
|---|---|
| mode | `settled` / `intraday` / `next_trading_day` |
| target_date | 估算目标交易日 |
| nav_prev_date | 基准净值日（必须是 target_date 的上一交易日） |
| session | 行情时段 `pre_open/trading/lunch/closed/non_trade_day` |
| label | 界面文案（如"今日盘中估算（2026-08-20）"） |
| is_official_published | 官方净值是否已公布 |

页面/推送不再自行推断，直接读取此字段。

---

## 11. PCB 三级信号（严格版，`core.pcb_signal_strength()`）

v3 旧实现只用"多窗口为正 + 10d>60d"两个宽松代理就可判强信号。v4 要求逐条证据：

| 代码 | 证据 | 判定条件 |
|---|---|---|
| C1 | 多窗口一致为正 | 所有窗口 β_pcb > 0 |
| C2 | 替代比例连续上升 | θ_pcb 近期线性斜率 > 0 |
| C3 | 增量验证 | 加入 PCB 因子后样本外 MAE 下降 |
| C4 | 独立证据 | 个股反推的 PCB 暴露 > 3% |
| C5 | 多篮子一致 | 不同 PCB 篮子结论同向（缺失视为未通过） |

**强信号** = C1 且 C2 且 C3 且 C4（且 C5 若可得）  
**中等信号** = C1 且 (C2/C3/C4 中至少 2 项)  
**弱信号** = C1 或部分条件  
**无信号** = 无数据

只有**强信号**才允许界面显示"PCB 调仓概率较高"；**不能显示"已调入 PCB"**。

---

## 12. 回测与验证体系（v4：扩样本 + 分市况 + 多指标 + 基线对照 + 选参/决策分离）

`tests/backtest_v3.py` 三大模式：

| 模式 | 用途 | 关键纪律 |
|---|---|---|
| 默认 | 最近 N 日滚动样本外回测（默认 60 日） | 只用正式收盘价（`core.load_settled_klines`），θ/β/权重/偏差修正一律只用历史 |
| `--tune` | 集成锐度选参：前 50% 选参 / 后 50% 独立验证 | 禁止同批数据既选参又验证；输出 Pareto 前沿分析 |
| `--decide` | 主口径决策：所有候选放在同一验证段、同一偏差修正条件下公平对比 | 双准则：准则A 全段MAE（样本最大）+ 准则B 最差子段MAE（抗崩）；检测排名翻转 |

**回测指标**：MAE / RMSE / 最大单日误差 / 方向准确率 / 平均偏差  
**分市况**：急涨(≥+1.5%) / 急跌(≤−1.5%) / 震荡 —— 暴露"大涨日滞后"等结构性缺陷  
**基线对照**：P1 静态基准逐项对照 —— 集成如果打不过 P1，多模型无存在意义

**关键回测发现（2026-08-21，112 评估日，验证段 36 日）**：
- 集成 闸门1.00(择优)：全段 MAE 0.5426%，最差子段 0.6220%，跨段波动 0.1588pp
- P4 单模型：全段 0.5393%，最差子段 0.6592%，跨段波动 0.2398pp
- P1 静态：全段 0.6084%，最差子段 0.6220%，跨段波动 0.0272pp
- **集成位于帕累托前沿，虽全段 MAE 与最优单模型统计无差异（-0.0033pp），但抗崩能力显著更强（跨段波动降低 34%）** → 主口径取集成 闸门1.00(择优)，单模型作稳健性参照
- 子段最优口径发生排名翻转 → 单段选口径必然过拟合，故决策改用双准则（全段 MAE + 最差子段 MAE）
- 偏差修正在 9/9 个口径上有效（整体有效，保留）

**改进验证脚本 `tests/validate_improvements.py`**：对比 v4_original（用 P3 误差偏差修正）vs v4_fixed（集成偏差 + 残差因子），验证核心修复效果。

---

## 13. 数据接口（含坑位）

| 数据 | 接口 | 坑位 |
|---|---|---|
| 前十大持仓 | 东方财富 `FundArchivesDatas.aspx?type=jjcc&code=006010&topline=10` | 需 Referer；td[6]=占净值% |
| 官方净值历史 | 东方财富 `api.fund.eastmoney.com/f10/lsjz` | 需 Referer；每页仅20条需分页；返回降序 |
| 实时行情 | 腾讯 `qt.gtimg.cn/q=sh/sz+代码` | **GBK 解码**；parts[3]=现价 parts[4]=昨收 |
| 历史日线 | 腾讯 `web.ifzq.gtimg.cn/appstock/app/fqkline/get` | 收在索引[2]（前复权）；东财 push2his 会限流 |
| 盘中快照 | `cache/intraday.json`（v4 新增） | 结构：`{"date", "settled", "prices", "unsettled_dates"}` |

---

## 14. 核心纪律（不变，v4 强化执行）

1. **无未来数据泄漏**：当天盘中只用 T−1 及以前数据；当日官方净值绝不参与当天参数估计；
2. **参数选择禁止用当天结果**：只能用预测日之前的数据（`--tune` 前段选参/后段验证强制执行）；
3. **β/θ 解释为有效暴露**，不解释为真实持仓；
4. **个股反推不用于认定持仓**：LASSO/NNLS 在相关股间解不唯一，个股结果须经多窗口/多候选池/多篮子稳定性检验；
5. **数据卫生**：盘中价不入 klines.json，只存 intraday.json；回测/回归/误差统计一律用清洗后的正式收盘价；
6. **集成审计**：每次输出完整权重计算轨迹，便于外部复核"为什么是这个权重"。

---

## 15. 输出格式（v4 增强字段，与 GUI/JSON 一致）

```text
006010 盘中估值 v4 · 多模型组合（可信度强化版）
==================================================
今日盘中估算（2026-08-20）    基准净值 0.5384 (2026-08-19)
------------------------------------------------------------------
  P1_Q2静态     +4.39%   (权重 48%)
  P2_调仓替代   +4.18%   (权重  4%)
  P3_行业因子   +4.36%   (权重 23%)
  P4_层级组合   +4.36%   (权重 24%)
  P5_个股辅助   +3.63%   (权重  1%)  [临时封顶 15%]
------------------------------------------------------------------
模型中心估计(加权): +4.35%
历史偏差修正(-0.12%): +4.23%  → 预计净值 ≈ 0.5601
经验预测区间: +3.49% ~ +5.21%  (±1.5σ=0.86%)
模型分歧: 0.68 pct   前十大覆盖率: 83.16%（实时兑现 83.16%/10只）
PCB有效替代比例: 7.1%  | PCB调仓信号: 中等信号
置信度: 78/100 (高)  明细 MAE35/40 分歧22/25 θ13/15 偏差8/10 数据10/10
主要风险: ①光通信~PCB相关r=0.72调仓比例识别误差 ②未披露持仓16.8%不可观测 ③盘中行情与基金估值时点存在时间差
==================================================

[主口径决策结论] 集成 闸门1.15 power2（跨段波动 0.1635pp < 单模型 0.2811pp，抗崩能力为核心价值）
```

`result.json` 关键新增字段（v4）：
- `estimation_mode/estimation_label/market_session/is_official_published` 显式估算语义
- `date_check_ok/date_problems` 日期自检结果
- `ensemble_audit` 完整集成审计轨迹
- `data_hygiene` 数据卫生留痕（intraday 分离、剔除未结算日期）
- `caliber_decision` 主口径决策报告引用
- `confidence_score/confidence_detail` 连续置信度分

---

## 16. 极简 GUI（fund_gui.py，v4 适配）

- 主数字：**偏差修正后中心**（涨红跌绿）+ 预计净值
- 信息：合理区间 / 置信度（0-100 分制明细）/ 基准净值 / 偏差修正
- 一行小字：P1~P5 各模型预测 + PCB 信号级别
- 「刷新」按钮重跑主脚本（UTF-8 管道 + 错误弹窗显示真实原因 + 日志 `cache/gui_refresh.log`）

**启动**：双击 `打开估值面板.bat`（自动建环境/装依赖/跑模型/开面板）。

---

## 17. 已知局限与后续增强

| 局限 | 说明 | 后续 |
|---|---|---|
| 光通信~PCB 共线 r≈0.72 | 调仓比例识别误差 | 引入正交化因子/更多行业篮子/残差因子建模 |
| PCB 篮子仅 3 只 | 代表性有限 | 扩展 PCB 指数/更多成分 |
| θ 上限需回测定 | 当前 35%/25% 为初始值 | 滚动回测网格选择 |
| 候选池有限 | Q2 后或有其他科技股 | 扩充候选池（AI算力/铜连接/其他光通信/半导体） |
| 打新/费用噪音 | 未净值净化 | 剔除打新收益（如可行） |
| 残差因子 | v4 引入简单 20 日均值修正 | 改用 Ridge/ElasticNet 在候选篮子上拟合（优化意见 §3） |
| fetch_all.py | ✅ 已修：双文件输出（klines.json 仅正式收盘价，intraday.json 存盘中快照） | 维护现有逻辑 |

---

## 18. 可复现性清单（交接/审计用）

- **代码唯一真源**：`src/core.py`（纯函数层）、`src/fund_valuation_v2.py`（生产主脚本）
- **回测/选参/决策**：`tests/backtest_v3.py`（三模式一体）
- **改进验证**：`tests/validate_improvements.py`
- **缓存产物**：`cache/nav.json`、`cache/klines.json`、`cache/intraday.json`、`cache/p5_mae.json`、`cache/result.json`、`cache/decide_report.json`、`cache/tune_report.json`、`cache/backtest_report.json`、`cache/validate_improvements_report.json`
- **参数锁定**：`core.py` 常量区（MAE_FLOOR=0.02, MAE_POWER=2.0, MAE_GATE=1.15, PROVISIONAL_WEIGHT_CAP=0.15 等）
- **版本标识**：`result.json` 中 `version: "v4"`，主脚本输出 `006010 盘中估值 v4`

---

## 19. 优化意见落实对照表（v4 完成状态）

| # | 优化意见条目 | v4 落实状态 | 关键代码/产物 |
|---|---|---|---|
| 1 | θ 没有真正平滑（prev 写死 prior） | ✅ 已修 | `core.fit_theta(prev=...)` 正确传递；主脚本/回测同源 |
| 2 | 偏差修正用 P3 误差而非集成自身 | ✅ 已修 | 主脚本重建集成历史预测→取误差；回测同纪律 |
| 3 | 增加共同残差因子 | 🟡 部分 | 引入 20 日残差均值修正（`validate_improvements.py` 验证有效）；后续改 Ridge/EN |
| 4 | P4 借用 P3 MAE，同源重复计权 | ✅ 已修 | P4 独立滚动样本外 MAE；集成归入 G3 分组去重 |
| 5 | P5 无真实 MAE 却分高权重 | ✅ 已修 | 真实 OOS MAE 0.718%（`p5_mae.json`）；`PROVISIONAL_WEIGHT_CAP=15%` |
| 6 | 盘中价污染历史收盘价 | ✅ 基建就绪 | `intraday.json` + `load_settled_klines()` 幂等清洗；`fetch_all.py` 已实现双文件输出（klines.json 仅正式收盘价，intraday.json 存盘中快照） |
| 7 | 目标日期用官方净值日致泄漏 | ✅ 已修 | `resolve_estimation_mode()` + `check_date_consistency()` 显式语义与自检；官方净值已发布 → 估算下一交易日，未发布 → 估算当前交易日 |
| 8 | P3 模型结构与 Q2 先验不一致 | 🟡 记录 | 当前保留行业因子形式；Q2 先验仍为光通信单一，残差因子将补齐 |
| 9 | 回测仅 3 天 | ✅ 已修 | 扩展至 112 日评估样本；分市况/多指标/基线对照/选参决策分离/前后段验证 |

---

## 20. 脚本用法汇总

```bash
# 数据更新（建议每日盘前/盘后各跑一次）
python fetch_all.py            # 抓净值(分页)+各篮子日线(腾讯)+干扰股，写 cache/

# 盘中估值（生产主脚本，v4）
python src/fund_valuation_v2.py    # 输出5项 + 前十大逐股 + 存 cache/result.json

# 个股辅助反推（并行参考）
python fund_holdings_infer.py  # LASSO+NNLS+Bootstrap + 存 cache/infer.json

# 回测验证
python tests/backtest_v3.py              # 默认最近 60 个交易日
python tests/backtest_v3.py --days 120   # 指定样本长度
python tests/backtest_v3.py --daily      # 额外打印逐日明细
python tests/backtest_v3.py --tune       # 集成锐度选参（前段选参/后段验证）
python tests/backtest_v3.py --decide     # 主口径决策（双准则+Pareto前沿）

# 改进验证
python tests/validate_improvements.py --days 120 --compare

# 可视化面板
python fund_gui.py             # 极简估值面板
# 或双击「打开估值面板.bat」（自动建环境→更新缓存→跑两模型→打开 GUI）
```

---

## 21. 交叉验证清单（PCB 调仓，持续观察）

1. **官方盘中估值对比**：天天基金/支付宝 App 看 006010 盘中估值，与本模型比较；若官方显著更低 → 印证已减光通信。
2. **连续 NAV 误差**：连续 3~5 个交易日做收盘回测（§12），看误差是否系统性偏离 0。
3. **重跑监测**：每天跑 `fund_valuation_v2.py`，观察 PCB 系数/θ_pcb 是否**连续数日**抬头（>0.05 即预警，当前已超）。
4. **最新持仓/报道**：搜"国融融银 许银丰 PCB / 沪电 / 胜宏"近期观点或季报前瞻。

> **结论**：因子模型已给出**调仓信号**（PCB 系数全窗口为正、短窗持续抬升、增量验证 MAE 下降、多篮子一致），当前为**中等信号**。受光通信~PCB 共线干扰，精确比例仍须用四条硬标准持续观察。

---