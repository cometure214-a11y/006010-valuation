# 006010 云端部署 · 优化说明（v3 · 手动刷新模式）

> v3 按用户需求改为**手动刷新 + 打开即刷新**，不再定时空跑。

## 刷新机制（三选一）

| 方式 | 做什么 | 何时用 |
|---|---|---|
| **打开页面** | 页面 JS 自动拉腾讯实时行情 → 立即显示"实时 Q2 估值"（含前十大逐股） | 随时随地，零成本 |
| **本地一键更新** | 双击「一键更新并推送.bat」→ 跑完整五模型 → git 推送 → 云端自动部署 | 想更新完整模型时（电脑前） |
| **GitHub 手动 Run** | 手机/电脑打开仓库 Actions 页 → Run workflow → 云端跑完整模型并部署 | 不在电脑前、想更新完整模型 |

页面结构：顶部=**实时估值**（打开即刷新，60秒自动重拉）；中部=**上次五模型完整快照**（含中心/区间/置信度/PCB信号/各模型）。

## workflow 变更（v3）

- **去掉 schedule 定时** → 改 `push` 触发（推送 docs 即部署，不重跑模型，几乎零消耗）
- 保留 `workflow_dispatch`（手动 Run 时云端全流程：抓取→双模型→生成→提交→部署）
- `push` 由 bot 提交时跳过（防冗余循环）

## 一、修复的问题

| # | 问题 | 修复 |
|---|---|---|
| 1 | **P5 永远读旧 infer.json**：原顺序 `v2 → infer` | workflow 与本地 GUI 顺序均改为 **`infer → v2`**，v2 读取最新 P5。实测 P5 权重 23% 生效 |
| 2 | 云端净值仅 120 条，历史偏短 | `fetch_cloud.py` min_rows 120 → **200 条** |

## 二、优化项

| # | 优化 | 效果 |
|---|---|---|
| 3 | 去掉定时 cron，改 push + 手动触发 | **云端零空跑**，只在你想更新时才耗 Actions 分钟 |
| 4 | setup-python 加 `cache: 'pip'` | 手动 Run 时依赖安装约 20 秒 |
| 5 | 页面 JS 实时拉行情（`qt.gtimg.cn` JSONP） | **打开页面即刷新**，60 秒自动重拉，不依赖云端 |
| 6 | 页面 `<meta refresh 600>` + 刷新按钮 | 双重刷新手段 |

## 三、国内访问加速

GitHub Pages（`*.github.io`）国内不稳定，备用：

**方案1（零配置）**：jsDelivr CDN 代理
```
https://cdn.jsdelivr.net/gh/<用户名>/<仓库名>@main/docs/index.html
```
> ⚠️ jsDelivr 缓存最长 12 小时，实时性要求高时用 Pages 原链接。

**方案2（推荐）**：绑定自有域名或 Cloudflare Pages——需要时我再帮你配。

## 四、验证记录（本地模拟）

```
fetch_cloud: nav 200条 / klines 3116条 / distract 8只 ✅
fund_holdings_infer → fund_valuation_v2: P1~P5 齐全，中心 +4.18% → 修正后 +3.98% ✅
gen_static: 页面含实时JS(loadQuotes/qt.gtimg.cn) + 快照区，JS语法校验通过 ✅
```

## 五、部署步骤（4 步）

1. GitHub 建公开仓库（如 `fund-006010`）
2. 推送 `cloud_deploy/` 全部文件到仓库根目录
3. Settings → Pages → Source 选 **GitHub Actions**
4. Actions → Run workflow 手动触发一次（首次生成完整快照）

首次运行后手机访问：
- GitHub Pages：`https://<用户名>.github.io/<仓库名>/`
- jsDelivr 加速：`https://cdn.jsdelivr.net/gh/<用户名>/<仓库名>@main/docs/index.html`
